

boost线程库
背包批量加物品，可堆叠。 判断能装不能装写复用代码。

好友系统：
想法1：
{
	DB:  
	{
		玩家 {id, relation_info， 其他玩家信息}
		关系{id1,id2,relation_type}
	}
	逻辑服务器：
	{
		relation_info 多个。 ---- 逻辑：需要访问，查找顺序（自身relation_info，在线玩家，读库），找到后就存relation_info。 上限后，删掉长期不用的。
							 ----玩家离线，保存一份relation_info。 在线玩家，不要relation_info。用代理方法，直接取在线玩家
							 --无须考虑存库。
		关系		----逻辑：加好友，删好友等。
					---- 存库
	}
}


最大小值列表。例如仇恨表： 需要指定最大，最小就可以，不用排序

GetCurrentPeriodNum 类扩展，支持从现在开始，设置多少秒为周期数。将来随时获取流逝周期数。（实际就是把目前有的功能，接口简单化）


只需要安装CMAKE的工程，研究如何搭建
CURL单独线程处理，多线程请求。
不用定时器的频率限制功能。比如1秒只能发送多次次事件。
测试 epollwait 那种循环rev,是不是会断消息
实现 zeromq demon代码
通用代码，读xml格式文件。 （方便实现配置文件）
用uint32范围做电表逻辑，假设流逝间隔少于最大表示范围。 任意先后刻度 能算出 间隔。
请求缓存，在另一事件处理读出来使用。 加锁（避免两次请求，第一次处理了第二次请求）

写reactor框架
写preactor(half-sync/half-async)框架

Leader/follower 模式

场景相关


聊天室复用代码
